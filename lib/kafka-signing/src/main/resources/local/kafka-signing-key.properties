# Local development signing key — DO NOT commit real keys here.
# Generate a test key pair:
#   openssl ecparam -name prime256v1 -genkey -noout -out ec-private.pem
#   openssl pkcs8 -topk8 -nocrypt -in ec-private.pem -outform DER | base64 -w0
PAW_SIGNING_PRIVATE_KEY_PKCS8_BASE64=REPLACE_WITH_BASE64_PKCS8_KEY
PAW_SIGNING_KEY_ID=local-ecdsa-v1
