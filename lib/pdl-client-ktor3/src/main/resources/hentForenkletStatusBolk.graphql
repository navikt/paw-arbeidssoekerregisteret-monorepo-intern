query($identer: [ID!]!) {
    hentPersonBolk(identer: $identer) {
        ident
        person {
            folkeregisterpersonstatus {
                forenkletStatus
            }
        }
        code
    }
}