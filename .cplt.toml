# Generated by `cplt init` — review before trusting.
# Detected: JVM (Gradle), Ktor, Flyway
# Workspace members:
#   buildSrc (heuristic scan) — JVM (Gradle)
#   lib/serialization (heuristic scan) — JVM (Gradle), Ktor
#   lib/logging (heuristic scan) — JVM (Gradle), Ktor
#   lib/hoplite-config (heuristic scan) — JVM (Gradle)
#   lib/pdl-client (heuristic scan) — JVM (Gradle), Ktor
#   lib/async (heuristic scan) — JVM (Gradle)
#   lib/tracing (heuristic scan) — JVM (Gradle)
#   lib/aareg-client (heuristic scan) — JVM (Gradle), Ktor
#   lib/security (heuristic scan) — JVM (Gradle), Ktor
#   lib/http-client-utils (heuristic scan) — JVM (Gradle), Ktor
#   lib/scheduling (heuristic scan) — JVM (Gradle), Ktor
#   lib/kafka (heuristic scan) — JVM (Gradle), Ktor
#   lib/kafka-key-generator-client (heuristic scan) — JVM (Gradle), Ktor
#   lib/health (heuristic scan) — JVM (Gradle), Ktor
#   lib/tilgangskontroll-client (heuristic scan) — JVM (Gradle), Ktor
#   lib/metrics (heuristic scan) — JVM (Gradle), Ktor
#   lib/kafka-streams (heuristic scan) — JVM (Gradle), Ktor
#   lib/api-oppslag-client (heuristic scan) — JVM (Gradle), Ktor
#   lib/kafka-hwm (heuristic scan) — JVM (Gradle)
#   lib/kafka-signing (heuristic scan) — JVM (Gradle)
#   lib/api-docs (heuristic scan) — JVM (Gradle), Ktor
#   lib/topics (heuristic scan) — JVM (Gradle)
#   lib/database (heuristic scan) — JVM (Gradle), Ktor
#   lib/error-handling (heuristic scan) — JVM (Gradle), Ktor
#   domain/bekreftelsesmelding-avro-schema (heuristic scan) — JVM (Gradle)
#   domain/bekreftelse-interne-hendelser (heuristic scan) — JVM (Gradle)
#   domain/arbeidssoeker-regler (heuristic scan) — JVM (Gradle), Ktor
#   domain/pdl-aktoer-schema (heuristic scan) — JVM (Gradle)
#   domain/error (heuristic scan) — JVM (Gradle), Ktor
#   domain/identitet-interne-hendelser (heuristic scan) — JVM (Gradle)
#   domain/main-avro-schema (heuristic scan) — JVM (Gradle)
#   domain/arbeidssoekerregisteret-kotlin (heuristic scan) — JVM (Gradle)
#   domain/felles (heuristic scan) — JVM (Gradle)
#   domain/interne-hendelser (heuristic scan) — JVM (Gradle)
#   domain/bekreftelse-paavegneav-avro-schema (heuristic scan) — JVM (Gradle)
#   apps/tilgangskontroll (heuristic scan) — JVM (Gradle), Ktor
#   apps/bekreftelse-tjeneste (heuristic scan) — JVM (Gradle), Ktor
#   apps/kafka-key-generator (heuristic scan) — JVM (Gradle), Ktor, Flyway
#   apps/min-side-varsler (heuristic scan) — JVM (Gradle), Ktor, Flyway
#   apps/dev-proxy (heuristic scan) — JVM (Gradle), Ktor
#   apps/hendelseprosessor (heuristic scan) — JVM (Gradle), Ktor
#   apps/bekreftelse-hendelsefilter (heuristic scan) — JVM (Gradle), Ktor
#   apps/api-start-stopp-perioder (heuristic scan) — JVM (Gradle), Ktor
#   apps/bekreftelse-api (heuristic scan) — JVM (Gradle), Ktor, Flyway
#   apps/hendelselogg-backup (heuristic scan) — JVM (Gradle), Ktor, Flyway
#   apps/bekreftelse-utgang (heuristic scan) — JVM (Gradle), Ktor
#   apps/bigquery-stats-adapter (heuristic scan) — JVM (Gradle), Ktor
#   apps/dolly-api (heuristic scan) — JVM (Gradle), Ktor
#   test/kafka-streams-test-functions (heuristic scan) — JVM (Gradle)
#   test/test-data-lib (heuristic scan) — JVM (Gradle)
#   jobs/arbeidssoekere-synk-jobb (heuristic scan) — JVM (Gradle), Ktor, Flyway

[propose]
# Suggested by: (root), apps/api-start-stopp-perioder, apps/bekreftelse-api, apps/bekreftelse-hendelsefilter, apps/bekreftelse-tjeneste, apps/bekreftelse-utgang, apps/bigquery-stats-adapter, apps/dev-proxy, apps/dolly-api, apps/hendelselogg-backup, apps/hendelseprosessor, apps/kafka-key-generator, apps/min-side-varsler, apps/tilgangskontroll, buildSrc, domain/arbeidssoeker-regler, domain/arbeidssoekerregisteret-kotlin, domain/bekreftelse-interne-hendelser, domain/bekreftelse-paavegneav-avro-schema, domain/bekreftelsesmelding-avro-schema, domain/error, domain/felles, domain/identitet-interne-hendelser, domain/interne-hendelser, domain/main-avro-schema, domain/pdl-aktoer-schema, jobs/arbeidssoekere-synk-jobb, lib/aareg-client, lib/api-docs, lib/api-oppslag-client, lib/async, lib/database, lib/error-handling, lib/health, lib/hoplite-config, lib/http-client-utils, lib/kafka, lib/kafka-hwm, lib/kafka-key-generator-client, lib/kafka-signing, lib/kafka-streams, lib/logging, lib/metrics, lib/pdl-client, lib/scheduling, lib/security, lib/serialization, lib/tilgangskontroll-client, lib/topics, lib/tracing, test/kafka-streams-test-functions, test/test-data-lib
allow_jvm_attach = true

[propose.allow]
ports = [5432]
localhost = [8080]

# The following are machine-specific — add to ~/.config/cplt/config.toml:
# allow.read += ["~/.gradle/gradle.properties"]  # in personal config

